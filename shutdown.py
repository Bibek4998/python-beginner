# The code to shutdown the pc ...
import os
os.system("shutdown /s /t 5") #Te character 's' in this code mean shut down and the character 't' mean time delay
# If you run the above code the computer will shutdown in 5 seconds ..